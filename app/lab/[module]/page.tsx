import { notFound } from "next/navigation";
import Link from "next/link";
import { LAB_MODULES, isLabModule, labFlags } from "@/lib/rxl/lab";
import { PerformanceProbe, SelfTest, StatusTools } from "@/components/rxl/lab/LabTools";
function ModuleNav() { return <nav className="rxl-lab-nav" aria-label="Lab modules"><Link href="/lab">Lab home</Link>{LAB_MODULES.map((item) => <Link href={`/lab/${item}`} key={item}>{item.replaceAll("-", " ")}</Link>)}</nav>; }
export default async function LabModulePage({ params }: { params: Promise<{ module: string }> }) { const { module } = await params; if (!isLabModule(module)) notFound(); const production = process.env.VERCEL_ENV === "production"; let content: React.ReactNode; if (module === "deployments") content = <><h1>Deployments</h1><p>Environment: <strong>{process.env.VERCEL_ENV || "development"}</strong></p><p>{production ? "Detailed deployment identifiers remain hidden in Production." : `Deployment ID: ${process.env.VERCEL_DEPLOYMENT_ID || "not available"}`}</p></>; else if (module === "observability") content = <><h1>Observability</h1><p>Generate bounded HTTP signals. Intentional 500 generation remains blocked in Production.</p><StatusTools production={production} /></>; else if (module === "preview-production") content = <><h1>Preview vs Production</h1><p>Preview shows its marker and diagnostic tooling. Production removes Preview UI and blocks unsafe probes.</p></>; else if (module === "environment") content = <><h1>Environment variables</h1><p>Only bounded environment state is displayed. Secrets are never rendered.</p><pre className="rxl-lab-result">{JSON.stringify({ VERCEL_ENV: process.env.VERCEL_ENV || "development", SANITY_CONNECTED: false }, null, 2)}</pre></>; else if (module === "feature-flags") content = <><h1>Feature flags</h1><p>Flags expose policy without claiming external integrations are live.</p><pre className="rxl-lab-result">{JSON.stringify(labFlags(), null, 2)}</pre></>; else if (module === "performance") content = <><h1>Performance</h1><p>Read the actual Vercel cache headers returned by the performance endpoint.</p><PerformanceProbe /></>; else content = <><h1>Error handling</h1><p>Preview self-test is available; unsafe Production tooling stays disabled.</p><SelfTest production={production} /></>; return <main id="main-content" className="rxl-lab-main"><ModuleNav /><section className="rxl-lab-module"><span className="rxl-lab-kicker">Engineering only · noindex</span>{content}</section></main>; }
